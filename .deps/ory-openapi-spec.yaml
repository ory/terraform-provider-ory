# Ory Network client OpenAPI spec that the project_config codegen reads.
# Source: https://github.com/ory/sdk/tree/master/spec/client (the file the
# published SDKs, including github.com/ory/client-go, are generated from).
# The spec is pinned here instead of derived from the client-go module version
# because the SDK publish job can stall while ory/sdk keeps receiving new specs.
# renovate: datasource=custom.ory-sdk-client-spec depName=ory-client-openapi-spec
version: v1.22.79
